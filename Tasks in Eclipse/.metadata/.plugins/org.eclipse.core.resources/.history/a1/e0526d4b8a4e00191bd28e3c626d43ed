package test;

import java.util.*;

import javax.lang.model.element.Element;

public class test1 {
	public void show(int x) {
		String s=Integer.toString(x);
		char[] ss = {"1","2"};
		List<char[]> l = Arrays.asList(ss);
		Collections.reverse(l);
		System.out.println(l);
	}
	
}
